name: Betopia Staging Website CI/CD
on:
  push:
    branches: ["staging"]
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Deploy to Server
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: "149.28.139.80"
        username: "root"
        password: "3-kTb!c((a%ca5v9"
        port: "22"
        script: "cd /var/www/betopia-website && ./.script/production.sh"
